---
description: Create a new Copilot Studio CLI agent project with the reusable create-copilot-studio-agent skill.
argument-hint: Agent description, optionally including display name, project directory, environment ID or URL, and publisher prefix
allowed-tools: Skill, Bash(pac), Read, Write, Glob, Grep, Task
---

# Create a Copilot Studio Agent

Initial request: $ARGUMENTS

Load and follow the **`create-copilot-studio-agent`** skill. Pass the complete initial request above
as the creation request. The skill is the authoritative workflow; do not duplicate, abbreviate, or
replace its initialization, structural validation, synchronization, error-handling, or publication
rules.
